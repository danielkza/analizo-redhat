Name:           perl-Graph-ReadWrite
Version:        2.07
Release:        1%{?dist}
Summary:        Modules for reading and writing directed graphs
License:        GPL+ or Artistic
Group:          Development/Libraries
URL:            http://search.cpan.org/dist/Graph-ReadWrite/
Source0:        http://www.cpan.org/authors/id/N/NE/NEILB/Graph-ReadWrite-%{version}.tar.gz
BuildRoot:      %{_tmppath}/%{name}-%{version}-%{release}-root-%(%{__id_u} -n)
BuildArch:      noarch
BuildRequires:  perl >= 0:5.006
BuildRequires:  perl(Carp)
BuildRequires:  perl(ExtUtils::MakeMaker)
BuildRequires:  perl(Graph) >= 0.5
BuildRequires:  perl(parent)
BuildRequires:  perl(Parse::Yapp::Driver)
BuildRequires:  perl(XML::Parser)
BuildRequires:  perl(XML::Writer)
Requires:       perl(Carp)
Requires:       perl(Graph) >= 0.5
Requires:       perl(parent)
Requires:       perl(Parse::Yapp::Driver)
Requires:       perl(XML::Parser)
Requires:       perl(XML::Writer)
Requires:       perl(:MODULE_COMPAT_%(eval "`%{__perl} -V:version`"; echo $version))

%description
This module is a placeholder in the Graph-ReadWrite distribution, which is
a collection of modules for reading and writing directed graphs.

%prep
%setup -q -n Graph-ReadWrite-%{version}

%build
%{__perl} Makefile.PL INSTALLDIRS=vendor
make %{?_smp_mflags}

%install
rm -rf $RPM_BUILD_ROOT

make pure_install PERL_INSTALL_ROOT=$RPM_BUILD_ROOT

find $RPM_BUILD_ROOT -type f -name .packlist -exec rm -f {} \;
find $RPM_BUILD_ROOT -depth -type d -exec rmdir {} 2>/dev/null \;

%{_fixperms} $RPM_BUILD_ROOT/*

%check
make test

%clean
rm -rf $RPM_BUILD_ROOT

%files
%defattr(-,root,root,-)
%doc Changes META.json README TODO
%{perl_vendorlib}/*
%{_mandir}/man3/*

%changelog
* Wed Apr 08 2015 Daniel Miranda <danielkza2@gmail.com> 2.07-1
- Specfile autogenerated by cpanspec 1.78.
